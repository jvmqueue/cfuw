define(['jQuery'], function($undefined){
    var _fnc = {
        parseXmlToJson:function(paramXml){
            console.group('PARSE XML TO JSON');
                console.log(':\t', 'Reached');
               console.groupEnd(); 
        }
    };
    return{
        fnc:_fnc
    }
});